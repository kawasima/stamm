# Dependencies & build output
node_modules/
dist/
*.tsbuildinfo

# SQLite database (and WAL/SHM sidecar files)
stamm.db
stamm.db-journal
stamm.db-wal
stamm.db-shm
*.sqlite
*.sqlite3
# the quickstart example writes here
quickstart.db
quickstart.db-journal
quickstart.db-wal
quickstart.db-shm
# the metrics example writes here
metrics.db
metrics.db-journal
metrics.db-wal
metrics.db-shm

# Seeded signing keys (contain private keys; written once on first boot)
stamm-seed-keys.json
*-seed-keys.json

# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# Logs
*.log

# OS files
.DS_Store
